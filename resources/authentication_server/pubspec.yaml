name: authentication_server
description: This is the backend for authenticating using web base interface 
version: 1.0.0

environment:
  sdk: ^3.0.3

dependencies:
  logging: ^1.2.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
